FRONTEND CHANGES
================

1) Add the renderStructuredPresentation() function from
   StructuredPresentation.jsx.txt immediately before:

   function App({ loginRequest, runtimeConfig }) {

2) In sendChatMessage(), find the assistant message object:

          text:
            body?.answer ||
            "The assistant returned no text response.",
          meta: {

Change it to:

          text:
            body?.answer ||
            "The assistant returned no text response.",
          presentation:
            body?.presentation || null,
          meta: {

3) In the message bubble, find:

{message.role === "assistant"
  ? renderAssistantText(message.text)
  : message.text}

Replace it with:

{message.role === "assistant"
  ? (
      message.presentation
        ? renderStructuredPresentation(
            message.presentation
          )
        : renderAssistantText(
            message.text
          )
    )
  : message.text}

4) Append the CSS from structured-response.css.txt to src/styles.css.

IMPORTANT:
Do NOT delete the old renderAssistantText parsers yet.
They remain as a fallback for text-only responses.
Once the structured contract is proven stable, they can be removed later.
